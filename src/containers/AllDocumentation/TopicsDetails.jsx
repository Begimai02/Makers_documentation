import { Typography } from "@material-ui/core";
import React from "react";

const TopicsDetails = () => {
  return (
    <div>
      <Typography variant="h4">Вступление в HTML</Typography>
    </div>
  );
};

export default TopicsDetails;
